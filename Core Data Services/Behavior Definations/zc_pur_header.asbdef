projection;
strict ( 2 );

use side effects;

define behavior for ZC_PUR_HEADER //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use association _ITEMS { create; }
}

define behavior for ZC_PUR_ITEM //alias <alias_name>
{
  use update;
  use delete;

  use association _HEADER;
}